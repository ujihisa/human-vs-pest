<!DOCTYPE html>
<html>
  <head>
    <title>Human vs Pest</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_import_module_tag 'live' if @world_tag %>
  </head>

  <body>
    <div class="container">
      <%= yield %>
    </div>
  </body>

  <br/><br/><br/>

  <nav class="navbar fixed-bottom bg-body-tertiary">
    <div class="container-fluid">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'マッチ一覧', game_matches_path, class: 'btn btn-secondary btn-sm' %>
          </li>
        </ul>
        <span class="navbar-text">
          あなたの名前: <strong><%= session[:you] || '未登録' %></strong>
          <%= link_to("名前設定画面を開く", '/you', class: 'btn btn-primary') unless session[:you] %>
        </span>
    </div>
  </nav>
</html>
